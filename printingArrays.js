var names = ["Hole-in-one!", "Eagle", "Birdie", "Par", "Bogey", "Double Bogey", "Go Home!"];

function printGolf(par, strokes) {
    
    for (var i = 0; i < names.length; i++) {
        // console.log(names[i]);
        // console.log(names);
        return console.log(names.toString());

    }
}

printGolf();

// can print the names array in multiple ways
// print the whole array with console.log()
// print the contents of the array via a for loop + console.log()
// print the contents of the array as a string, use 'names.toString()'